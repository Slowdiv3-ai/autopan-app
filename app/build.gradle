    signingConfigs {
        release {
            storeFile file("autopan.keystore")
            storePassword "autopan123"
            keyAlias "autopan"
            keyPassword "autopan123"
        }
    }
